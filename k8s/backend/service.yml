apiVersion: v1
kind: Service
metadata:
  name: russchords-backend-service
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.220
  selector:
    app: russchords-backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
